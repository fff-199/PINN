# 苯甲醚蒸发模型技术详解与验证报告

## 1. 模型背景与验证目标

本技术文档旨在详细阐述 `evaporation_flat_plate_correlation.py` 脚本中的物理计算逻辑，并解释为何该模型的计算结果（较低的蒸发量）比早期模型更符合 COMSOL 模拟结果及物理事实。

**核心差异点**：
*   **早期模型**：基于 **Dittus-Boelter** 关联式（管内满流湍流假设）。
*   **当前模型**：基于 **Flat Plate Boundary Layer** 理论（平板边界层流假设），并修正了扩散系数 $D_{AB}$。

---

## 2. 详细计算流程 (Step-by-Step Calculation)

以下计算步骤以 **Zone 1 ($40^\circ C$)** 为例进行演示。

### 步骤 1: 基础热力学参数
首先计算溶剂在给定温度下的饱和状态。

*   **饱和蒸汽压 ($P_{sat}$)**
    使用 Antoine 方程：
    $$ \log_{10}(P_{bar}) = A - \frac{B}{T_K + C} $$
    $$ P_{sat} (Pa) = 10^{\log P} \times 10^5 $$
    *   参数：$A=4.177, B=1489.7, C=-69.6$
    *   结果 (@40C)：$P_{sat} \approx 1149 \, Pa$

*   **饱和浓度 ($C_{sat}$)**
    基于理想气体状态方程 ($PV=nRT$)：
    $$ C_{sat} = \frac{P_{sat} \cdot M_{sol}}{R \cdot T_K} $$
    *   结果 (@40C)：$C_{sat} \approx 0.0477 \, kg/m^3$

### 步骤 2: 气相物性参数
计算作为载气的氮气 ($N_2$) 的性质。

*   **气体密度 ($\rho_{gas}$)**
    $$ \rho_{N2} = \frac{P_{std} \cdot M_{N2}}{R \cdot T_K} \approx 1.09 \, kg/m^3 $$

*   **运动粘度 ($\nu$)**
    $$ \nu = \frac{\mu}{\rho_{gas}} $$
    *   结果 (@40C)：$\nu \approx 1.69 \times 10^{-5} \, m^2/s$

### 步骤 3: 扩散系数 (Fuller Method 修正)
这是本次修正的重点。使用 Fuller-Schettler-Giddings (1966) 方法估算二元气体扩散系数。

$$ D_{AB} = \frac{0.00143 \cdot T^{1.75}}{P_{bar} \cdot M_{AB}^{1/2} \cdot [(\Sigma v)_A^{1/3} + (\Sigma v)_B^{1/3}]^2} $$

*   **扩散体积 ($\Sigma v$)**：
    *   $N_2$: 17.9
    *   苯甲醚 ($C_7H_8O$): $7(16.5) + 8(1.98) + 5.48 \approx 136.8$
*   **计算结果 (@25C/298K)**：
    *   $D_{AB} \approx 0.76 \times 10^{-5} \, m^2/s$ (原代码使用 $0.70 \times 10^{-5}$)
    *   该修正使传质效率提升了约 9%。

### 步骤 4: 流态判定 (Reynolds Number)
这是决定计算结果数量级的**最关键步骤**。

*   **特征长度 ($L$)**：取单温区长度 $L = 8.0 \, m$。
*   **流速 ($V$)**：$V_{eff} \approx 0.69 \, m/s$。
*   **雷诺数 ($Re_L$)**：
    $$ Re_L = \frac{V \cdot L}{\nu} $$
    *   计算值：$Re_L \approx 3.3 \times 10^5$

**物理判定**：
对于平板流动，**临界雷诺数** ($Re_{crit}$) 通常为 $5 \times 10^5$。
$$ Re_L (3.3 \times 10^5) < Re_{crit} (5 \times 10^5) $$
**结论**：气流在流经 Zone 1 的整个过程中，并未发展成完全湍流，而是处于**层流 (Laminar) 或过渡态**。

### 步骤 5: 传质关联式 (Sherwood Number)
根据流态判定，必须选择正确的关联式。

*   **错误选择 (旧模型 - 管流湍流)**：
    $$ Sh = 0.023 Re^{0.8} Sc^{0.4} \Rightarrow h_m \approx 0.0012 \, m/s $$
    *   *导致高估 2.5 倍*。

*   **正确选择 (新模型 - 层流/混合)**：
    由于 $Re < Re_{crit}$，使用层流平均关联式：
    $$ Sh = 0.664 \cdot Re_L^{0.5} \cdot Sc^{1/3} $$
    *   计算值：$Sh \approx 524$

### 步骤 6: 传质系数与蒸发通量
$$ h_m = \frac{Sh \cdot D_{AB}}{L} $$
$$ Flux = h_m \cdot (C_{sat} - C_{bulk}) $$

*   **最终传质系数 ($h_m$)**：$\approx 0.00050 \, m/s$
*   **对比**：这个值远低于旧模型的 0.0012 m/s，直接导致并解释了 COMSOL 模拟中较低的蒸发量。

---

## 3. 为什么这个结果更合理？

### 3.1 几何尺度的影响
在管流模型中，特征尺度是水力直径 ($D_h \approx 1m$)。在平板模型中，特征尺度是长度 ($L=8m$)。
$$ h_m \propto \frac{1}{L} $$
即使流态相同，更长的特征尺度也意味着**边界层更厚**（见图2），扩散阻力更大，传质系数更低。

### 3.2 边界层的物理图像
*   **管流假设**：假设烘箱内全是强烈的涡旋，溶剂分子一离开表面就被卷走。
*   **平板实况**：气流平滑掠过表面，溶剂分子必须穿过一层静止或缓慢移动的气膜（层流边界层）。
*   **COMSOL 验证**：COMSOL 的 $k-\epsilon$ 模型虽然名为湍流模型，但在低雷诺数下会自动退化为近似层流的计算结果，这与我们的 $Re < 5 \times 10^5$ 判定不谋而合。

---

## 4. 绘图结果说明

1.  **图1（干燥动力学）**：
    红色实线显示残留溶剂随位置的变化。由于使用了层流模型，曲线下降较缓。这提示在当前的低风速下，依靠自然挥发很难达到 5% 的干燥目标。

2.  **图2（浓度场热图）**：
    展示了 $C(x,y)$ 的分布。
    *   **白色虚线**：标示了计算出的边界层厚度 $\delta(x) \propto \sqrt{x}$。
    *   **物理意义**：大部分溶剂被“封锁”在靠近膜表面的边界层内（暖色区域），并没有扩散到烘箱上部空间（冷色区域）。这再次印证了为什么蒸发慢。

3.  **图3（参数柱状图）**：
    直观对比了三个温区的参数。注意左下角的 $Re$ 数值，均在 $2.5 \sim 3.5 \times 10^5$ 之间，明确指示了非湍流状态。

---

## 5. 优化建议

如果目前的蒸发效率 ($~33\%$) 无法满足生产要求，基于上述物理分析，建议：

1.  **增加风速**：提高 $V$ 可以直接提高 $Re$，一旦突破 $5 \times 10^5$，进入湍流区，传质系数会有跃升 ($Re^{0.5} \to Re^{0.8}$)。
2.  **改变风嘴结构**：使用垂直冲击射流 (Impingement) 代替平行流，可以人为打破边界层，这是提升干燥效率最直接的手段。

---

## 6. 模拟结果汇总 (Simulation Results)

基于 `evaporation_flat_plate_correlation.py` 的运行结果：

### 6.1 模型配置
*   **物理模型**: Mixed/Laminar Flat Plate Correlation ($Re < 5 \times 10^5$)
*   **特征长度**: 8.0 m (温区长度)
*   **有效蒸发面积**: 19.20 $m^2$ (双面)

### 6.2 温区逐级与最终结果

| Parameter | Zone 1 ($40^\circ C$) | Zone 2 ($60^\circ C$) | Zone 3 ($80^\circ C$) | Total / Final |
| :--- | :--- | :--- | :--- | :--- |
| **入口质量** | 360.00 g | 349.01 g | 317.25 g | **360.00 g** |
| **出口质量** | 349.01 g | 317.25 g | 238.29 g | **238.29 g** |
| **去除效率** | 3.05% | 9.10% | 24.89% | **33.81%** |
| **蒸发通量** | 0.0239 $g/(m^2\cdot s)$ | 0.0689 $g/(m^2\cdot s)$ | 0.1713 $g/(m^2\cdot s)$ | - |
| **传质系数 ($h_m$)** | 0.00050 m/s | 0.00053 m/s | 0.00056 m/s | - |
| **雷诺数 ($Re_L$)** | $3.3 \times 10^5$ | $2.9 \times 10^5$ | $2.7 \times 10^5$ | - |

### 6.3 结论
*   **最终剩余质量**: 238.29 g
*   **总去除效率**: 33.81%
*   **目标状态**: ❌ **FAIL** (剩余量 > 5%)
